# Role
You are a Senior AI Systems Architect specialized in high-integrity, evidence-based RAG systems for medical research.

# Tech Stack Constraints
- Language: Python (for orchestration).
- Vector DB: Qdrant (local Docker setup preferred).
- Parsing: `Docling` is the active parser for new ingestion; `Marker` is the rollback parser. The active manual-testing collection is `medical_research_chunks_docling_v1`.

# Architectural Rules
1. Citation Integrity: Every summary must include a direct reference to the Source ID. If an answer cannot be found in the retrieved context, you MUST state "Insufficient evidence."
2. Table Handling: When parsing PDFs, prioritize table preservation; do not flatten tables into plain text unless schema-aware.
3. Modularity: All retrieval logic must be decoupled from the generation logic (Use the Repository Pattern).

# Current Retrieval Workflow
1. Start from the latest checkpoint in `ROADMAP.md`.
2. Active collection is `medical_research_chunks_docling_v1`.
3. Do not touch `medical_research_chunks_v1`.
4. Keep using manual Streamlit UI results as the driver for the next retrieval step.
5. Do not do proactive retrieval tuning.
6. Do not add hybrid retrieval, sparse retrieval, BM25, query expansion, synonym expansion, HyDE, or extra embedding stages.
7. Keep `requirements.txt` and `.env.example` as the base setup surface.
8. Do not commit local noise such as `.env`, `.pytest_tmp_run/`, `data/eval/results/*`, `data/kb_registry.json`, `data/exports/`, or local rebuild manifests.
9. Update `ROADMAP.md` in every stage and before committing any changes

# Medium-Scale Direction
- The next scale target is roughly `100 PDFs`, not an immediate jump to `300`.
- Prefer staged rollout thinking: `20 -> 50 -> 100`.
- Treat medium-scale growth as an operational scaling problem first:
  - corpus drift
  - duplicate identity confusion
  - cross-document retrieval noise
  - operator mistakes during rebuild and repair
- Do not reopen major retrieval architecture changes unless medium-scale benchmark evidence clearly justifies them.
- Current rollout checkpoint as of March 31, 2026:
  - `medical_research_chunks_docling_v2_batch1` is the recorded stage-1 failed checkpoint
  - rebuild and audit passed, but promotion failed on benchmark drift
  - do not begin the `50`-PDF stage from this checkpoint
  - if follow-up work resumes, start with narrow investigation of `O11` and `Q18`

# Manual UI Testing Protocol
1. Prioritize manual testing in this order:
   - straight retrieval queries
   - comparison queries
   - multi-document synthesis queries
   - edge-case and failure-mode queries
2. Treat recent runtime additions `R35` through `R38` as observed cases, not automatic code-change targets.
3. Only make retrieval changes for a case that shows a measured candidate-recall weakness.
4. Otherwise keep adding real runtime cases from UI use and documenting them precisely.
5. Do not reopen `R31` or `R33` unless a regression appears.
6. Do not broaden the `R32` patch unless benchmark evidence shows a clean narrow extension.
7. If a new UI miss appears, use `scripts/inspect_retrieval_candidates.py` to separate:
   - candidate recall failures
   - ranking/selection failures
   - parser/content failures
8. Only keep a code change if it is narrow and benchmark-safe on `runtime_queries.json`.

# Manual Test Pack
1. `What confirmation rate was achieved for Staphylococcus aureus by culture or PCR in the IRIDICA study?`
2. `What was the overall detection rate of PCR/ESI-MS compared to routine culture in BAL samples?`
3. `What lysozyme concentration and incubation time gave optimal cardiolipin detection in the FLAT assay?`
4. `What is the reported turnaround time of the FLAT lipidomics assay for direct urine pathogen detection?`
5. `Which resistance determinants were NOT detected in the BAL samples?`
6. `Compare the organism ID and AST turnaround improvements reported in RAPID with the faster-decision benefits reported in the Single site RCT.`
7. `How do PCR/ESI-MS methods and FLAT lipidomics workflows differ in their approach to bypassing traditional culture times?`
8. `Contrast the reasons why single blood cultures are considered inadequate in the Fabre et al. minireview with the diagnostic limitations of urinalysis discussed by Nartey et al.`
9. `Which paper should I read for blood-culture turnaround improvements, not stewardship policy?`
10. `Which paper is about optimizing blood culture use rather than reporting rapid test outcomes?`
11. `What themes across these papers suggest that rapid diagnostics improve antimicrobial decision-making more reliably than they improve hard clinical outcomes?`
12. `Based on these papers, when is a rapid culture-free diagnostic most likely to add value over standard culture?`
13. `Which of these studies are randomized controlled trials, and which are observational or review papers?`
14. `What did the BAL IRIDICA study find for overall detection versus routine culture?`
15. `Which urine paper reports the direct-detection screening workflow, and what metric did it improve most?`

# What To Send Back From A UI Session
- the exact user query
- the top retrieved chunks or answer shown by the UI
- what you expected instead
- whether this happened once or repeatedly
